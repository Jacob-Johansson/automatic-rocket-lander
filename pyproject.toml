[project]
name = "automatic-rocket-lander"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jacob Johansson", email = "jacob99.e.g.j@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
automatic-rocket-lander = "automatic_rocket_lander:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
